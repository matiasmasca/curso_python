# Tuples < "tapl" > - Tuplas.
# tipo de escrutura de dato, contenedor para guardar diferetnes tipos de valores, similar a las listas
# pero es una lista INMUTABLE, no se puede cambiar o modificar una vez creada.

cordenadas = (99, 1)
print(cordenadas)
print(cordenadas[0])
print(cordenadas[1])

# error, si queres modificar
# cordenadas[0] = 1

# podes crear una lista de tuplas

cordenadas = [(99, 1), (5,0), (4,5)]
